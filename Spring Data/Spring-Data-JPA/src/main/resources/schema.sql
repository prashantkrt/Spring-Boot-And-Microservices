-- Create Table
-- Drop Table
-- Insert Table

-- Used for creating database schema (tables, indexes, constraints, etc.).
-- Ideal when you want to define the structure of your database explicitly instead of relying on automatic schema generation by JPA.
-- Typically contains CREATE TABLE, ALTER TABLE, and other DDL (Data Definition Language) statements.


-- Use schema.sql when you need explicit control over the schema or are not relying on Hibernate for schema generation.